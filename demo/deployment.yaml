apiVersion: apps/v1beta1
kind: Deployment
metadata:
    name: demo
spec:
    replicas: 1
    template:
        metadata:
            labels:
                app: api
        spec:
            containers:
                - name: api
                  image: allankerr/api
                  ports:
                      - containerPort: 8080
